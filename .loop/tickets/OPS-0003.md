# OPS-0003: Public repository hardening

Status: Done
Owner session: Control Session
Review session: QA / Regression Session
Worktree: `worktrees/control/OPS-0003`

## 1. Objective

Add public repository policy, license/notice/security files, and public-readiness static checks.

## 2. Required read set

- `AGENTS.md`
- `README.md`
- `operations/manifest.md`
- `operations/loop_policy.toml`
- `docs/02_document_index.md`
- `docs/81_human_operator_minimal_steps.md`
- `prompts/60_final_bootstrap_prompt.md`
- `.loop/gates/GATE-OPS-0000-migration-ready.md`

## 3. Dependencies

| Dependency | Required state |
|---|---|
| OPS-0002 | Done |

## 4. Scope

- Keep gameplay implementation tickets out of Ready state during operations migration.
- Update only operations, docs, prompts, scripts, gates, tickets, and reports needed by this OPS ticket.
- Preserve the no-API-key worker policy: GitHub Actions remains a verifier/gate/controller, not an AI worker.
- Preserve session and worktree separation evidence requirements.

## 5. Required checks

```bash
scripts/check_bootstrap_consistency.sh
scripts/ci_local_equivalent.sh --static-only
scripts/check_public_repository_static.py
scripts/list_ready_tickets.sh
```

## 6. Acceptance criteria

- Exactly one ticket had `Status: Ready` while this OPS ticket was active.
- The Ready ticket is `OPS-0003` while this OPS ticket is active.
- `TKT-0005` and all gameplay implementation tickets remain Blocked until the operations migration gate passes.
- The PR includes command evidence from GitHub Actions compatible checks.
- No OpenAI API key or Codex API key is introduced as a GitHub Actions requirement.
- Public-readiness policy, license, notice, third-party notice, security policy, and Dependabot files exist.
- `pull_request_target`, `openai/codex-action@v1`, committed asset payloads, and secret literals are rejected by `scripts/check_public_repository_static.py`.

## 7. Evidence required

- Implementation plan.
- Command log.
- Static-only CI output.
- Changed-file summary.
- Remaining risk list.
- QA / Regression Session verdict.

## 8. Next-ticket transition evidence

When `OPS-0003` is Done, the controller may promote `OPS-0004` only after this ticket's required checks and review evidence pass. No gameplay implementation ticket may be promoted before `OPS-0009` is Done and `GATE-OPS-0000` has passed.
