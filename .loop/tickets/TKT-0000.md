# TKT-0000: Spec repair gate and bootstrap consistency check

Status: Blocked
Owner session: Ticket Implementation Session
Review session: Design Review Session
Worktree: `worktrees/impl/TKT-0000`

## 1. Objective

Preserve the original Step6 autonomy scorecard bootstrap validation for post-migration replay. Verify that compatibility contract, OpenTaiko research, coverage design, ticket backlog, prompts, gates, canonical crate/binary names, and autonomous loop scorecard are aligned before implementation starts.

## 2. Required read set

- `AGENTS.md`
- `.loop/gates/GATE-0000-spec-repair.md`
- `docs/00_project_objective.md`
- `docs/05_autonomy_scorecard.md`
- `docs/06_gate_transition_rules.md`
- `docs/07_failure_feedback_protocol.md`
- `docs/40_loop_cli_contract.md`
- `docs/70_phase1_ticket_backlog.md`
- `docs/73_first_execution_batch.md`
- `prompts/60_final_bootstrap_prompt.md`

## 3. Dependencies

| Dependency | Required state |
|---|---|
| none | none |

## 4. Scope

- Validate `GATE-0000` inputs.
- Check no implementation ticket is Ready before gate pass.
- Check crate name consistency.
- Check prompt read sets.
- Check autonomy scorecard and gate transition readiness.
- Produce `.loop/session_logs/GATE-0000-report.md`.

## 5. Required checks

`TKT-0000` runs before Rust workspace and `taiko_cli` exist. Therefore this ticket must use repository-local bootstrap checks only.

```bash
scripts/check_bootstrap_consistency.sh
scripts/check_reference_integrity.py
scripts/validate_fixture_manifest.py
scripts/validate_no_user_assets.sh
scripts/check_autonomy_scorecard.py
scripts/list_ready_tickets.sh
```

`taiko_cli loop ...` commands become valid only after `TKT-0001` creates the workspace and CLI crate.

## 6. Acceptance criteria

- `TKT-0000` remains Blocked during the operations migration; `OPS-0001` is the only Ready ticket.
- No stale `taiko_core` crate reference remains as canonical name.
- Gate report records pass/fail for all required checks.
- Gate report records autonomy scorecard axis deltas.
- Gate report identifies the next-ticket transition and explains why `TKT-0001` remains Blocked until `GATE-0020` passes.

## 7. Evidence required

- Implementation plan.
- Plan review result from Design Review Session.
- Command log.
- Bootstrap consistency report, reference integrity report, fixture manifest validation report, and user asset validation report.
- Final review result.
- Remaining risk list.

## 8. Next-ticket transition evidence

When the operations migration is complete and `TKT-0000` is later selected, the Control Session may record `GATE-0000` as passed only if all required checks pass and `.loop/session_logs/GATE-0000-report.md` exists. `TKT-0001` must remain Blocked until `GATE-0010` and `GATE-0020` also pass. The next selected work after `TKT-0000` is gate completion, not gameplay implementation.
