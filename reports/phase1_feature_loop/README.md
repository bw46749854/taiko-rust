# Phase1 Feature Loop Reports

This directory is reserved for generated Phase1 feature-loop JSON reports.

Expected generated files:

- `phase1_feature_validate.json`
- `phase1_feature_plan.json`

The bootstrap package does not include pass evidence reports. Gate reports must be produced by a Control Session and reviewed by QA / Regression Session.
