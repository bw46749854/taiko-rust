# Failure reports

This directory stores Markdown failure reports that follow `templates/failure_report_template.md`.

Step11 includes one synthetic sample report so the failure feedback command surface can be validated without requiring a real QA failure first. The sample is not evidence that a current ticket failed; it is an executable contract fixture for failure-to-ticket routing.
