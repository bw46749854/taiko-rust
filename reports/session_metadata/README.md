# Session metadata reports

Status: canonical

This directory stores machine-readable session-separation metadata for loop PRs.

Canonical file pattern:

```text
reports/session_metadata/<ticket-id>.toml
```

These files are generated by `taiko_cli loop run-once --mode apply` and validated by `scripts/check_session_separation.py`.
